'use client';
import { Container, TextField } from '@mui/material';
import React from 'react';

export default function Explore() {
    return (
        <Container sx={{ background: '#fff' }}>
            <TextField sx={{ borderRadius: '30px' }} />
        </Container>
    );
}
